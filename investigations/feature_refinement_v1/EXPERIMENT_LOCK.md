# Target feature refinement, no pseudo labels or Flow

The three already trained CE-only A controls at seeds 202601/202602/202603 are reused. Train new F1 and F2 runs from scratch at exactly these seeds under the same Houston13→Houston18 official ILDA, source 180/class split, GT>0 target sampling/order, 7x7 patch, batch 32, 38 updates/epoch, official radiation-noise/flip passes, SGD/LR schedule, momentum, weight decay, 100 epochs and source-val-best checkpoint rule as A. Target GT supplies coordinates to reproduce the official sampling, never a loss or selection decision. Do not tune on Houston18 GT. Five-epoch smoke runs may check finite gradients and protocol only; no target-GT scoring in smoke runs.

F1 adds to source CE a target feature view-invariance loss: `mean(1-cosine(stopgrad(normalize(z_target_weak)),normalize(z_target_radiation_noise)))`. The weak view is the unaltered target patch and the strong view uses the existing official radiation-noise augmentation. F2 adds only a variance floor on the normalized strong target features: `sqrt(288)*mean(relu(0.5/sqrt(288)-sqrt(var_batch_dimension+1e-4)))`. The floor is a predeclared scale-normalized constant, not estimated from target labels or source class proportions. Both losses have fixed coefficient 1 and the same `min(epoch/10,1)` ramp; F1 omits the variance term. No EMA, target pseudo-label, HyperSIGMA gradient, prototype loss, JCPOT or Flow enters training. Six DCRN forward passes and their BN update order match A; the target strong pass now has gradient.

Save all 100 epoch checkpoints/history, source-val-best checkpoint, protocol hashes and official test predictions. Post-hoc, evaluate A/F1/F2 both raw and with the **same frozen HyperSIGMA all-53,200 prior** and existing soft-KL lambda=1 diagnostic. The lambda=1 point came from prior Houston18 development and is not independently validated. Report OA (`correct/53200`), AA, Kappa, each class recall, especially class 7, three-seed mean ± sample SD, soft class mass and class-7 candidate retention at the fixed measurement gate 0.8. Do not promote a new method based only on this already-inspected scene or a single OA number. If F1/F2 improves target class structure, run the same predeclared JCPOT two-reg 8,192-prefix coupling audit before any Flow experiment.
